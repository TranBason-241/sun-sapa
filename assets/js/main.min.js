$(document).ready(function () {
  function reply_click(clicked_id) {
    alert(clicked_id);
  }
});
let showDetail = (detailId, buttonId, sectionId) => {
  // showdetail
  document.getElementById(detailId).style.display = "block";
  // document.getElementById(detailId).classList.add("section_1_detail_active");
  // hide button
  document.getElementById(buttonId).style.display = "none";
  var ele = document.getElementById(sectionId);

  let scrollSize = 0;
  let isMobile = window.matchMedia(
    "only screen and (max-width: 414px)"
  ).matches;

  if (isMobile) {
    scrollSize = 200;
  } else {
    scrollSize = 600;
  }

  window.scrollTo(ele.offsetLeft, ele.offsetTop + scrollSize);
  // margin
  if (sectionId == "section_1") {
    document.getElementById("section_2").classList.remove("section_2_mt");
  }
  if (sectionId == "section_2") {
    document.getElementById("section_3").classList.remove("section_mt");
  }
  if (sectionId == "section_3") {
    document.getElementById("section_4").classList.remove("section_mt");
    if (isMobile) {
      document.getElementById("section_4").classList.add("section_4_mt");
    }
  }

  AOS.refresh();
};

let hideDetail = (detailId, buttonId, sectionId) => {
  // showdetail
  document.getElementById(detailId).style.display = "none";
  // show button
  document.getElementById(buttonId).style.display = "block";
  //scroll to model
  var ele = document.getElementById(sectionId);
  window.scrollTo(ele.offsetLeft, ele.offsetTop);
  // margin
  if (sectionId == "section_1") {
    document.getElementById("section_2").classList.add("section_2_mt");
  }
  if (sectionId == "section_2") {
    document.getElementById("section_3").classList.add("section_mt");
  }
  if (sectionId == "section_3") {
    document.getElementById("section_4").classList.add("section_mt");
  }
  AOS.refresh();
};
